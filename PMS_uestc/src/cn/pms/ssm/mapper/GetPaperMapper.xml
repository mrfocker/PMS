<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.pms.ssm.mapper.GetPaperMapper">

	<select id="selectPaperInfo" resultType="PaperVo" parameterType="cn.pms.ssm.po.PaperVo">
		SELECT
		Paper_stuId,Paper_title,Paper_researchOne,Paper_researchTwo,Stu_teacherId,
		GROUP_CONCAT(B.Teacher_id) as Judge_teacherIdOne
		FROM paper
		JOIN student
		ON
		paper.Paper_stuId=student.Stu_id
		left JOIN blindjudgegroup as B
		ON paper.Paper_stuId=B.Stu_id
		GROUP BY Paper_stuId
	</select>
</mapper>