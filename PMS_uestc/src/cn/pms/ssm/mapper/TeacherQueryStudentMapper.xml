<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="cn.pms.ssm.mapper.TeacherQueryStudentMapper">

	<!--导师查询自己学生的论文信息-->
<select id="findMyStuInfo" parameterType="cn.pms.ssm.po.TeacherQueryVo" resultType="cn.pms.ssm.po.TeacherQueryVo">
		SELECT
		  paper.Paper_title,
		  paper.Paper_researchOne,
		  paper.Paper_researchTwo,
		  paper.Paper_researchThree,
		  student.Stu_name,
		  student.Stu_id,
		  teacher.Teacher_id,
		  teacher.Teacher_name
		FROM 
		  paper, 
		  student,
		  teacher
		WHERE 
		  paper.Paper_stuId = student.Stu_id AND teacher.Teacher_id = student.Stu_teacherId
</select>

<update id="updateTeacherAdvise" parameterType="cn.pms.ssm.po.TeacherQueryVo" >
    update 
      paper
    set 
      Paper_ifpass = #{Paper_ifpass},
      Paper_advise = #{Paper_advise}
    where student.Stu_id = paper.Paper_StuId
</update>
</mapper>